# Instructions for AI Agents

Welcome, AI Agents (Claude Code, Cursor, Copilot, etc.). This file outlines conventions for the `meryem_guclu` portfolio repository.

## Project Structure
- `index.html`: Main portfolio page. Uses semantic HTML5, contains structured data (JSON-LD).
- `style.css`: Styling for the portfolio.
- `script.js`: Interactive logic.

## Conventions
- This is a static HTML/CSS/JS site.
- No build step is required.
- Do not introduce a framework (like React or Vue) without explicit user consent.
- Keep changes minimal and maintain the existing design language.
- Ensure any added content is factual; do not hallucinate projects or skills.

## Commands
- **Build**: N/A (Static site)
- **Test**: N/A
